meta {
  name: Readyz
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/readyz
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("status body is ok", function () {
    expect(res.getBody().status).to.equal("ok");
  });
}

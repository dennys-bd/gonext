meta {
  name: Get Current User (No Session)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/users/me
  body: none
  auth: none
}

assert {
  res.status: eq 401
}

tests {
  test("error detail reports that authentication is required", function () {
    expect(res.getBody().detail).to.include("authentication required");
  });
}

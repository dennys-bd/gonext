# This is pnpm's per-project settings file (not a multi-package workspace
# declaration — there is no `packages:` key). In pnpm 10+, build-script
# approval settings live here. Auto-generated by `create-next-app`.
allowBuilds:
  sharp: false
  unrs-resolver: false

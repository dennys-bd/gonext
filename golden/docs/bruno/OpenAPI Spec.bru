meta {
  name: OpenAPI Spec
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/openapi.json
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("has OpenAPI components", function () {
    expect(res.getBody()).to.have.property("components");
  });
}

meta {
  name: Delete Product
  type: http
  seq: 23
}

delete {
  url: {{baseUrl}}/products/{{productId}}
  body: none
  auth: none
}

headers {
  Cookie: {{exampleSessionCookie}}
}

assert {
  res.status: eq 204
}

meta {
  name: Create Widget (Missing Field)
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/widgets
  body: json
  auth: none
}

body:json {
  {}
}

assert {
  res.status: eq 422
}

tests {
  test("lists the missing fields", function () {
    expect(res.getBody().errors).to.be.an("array").and.not.empty;
  });
}
